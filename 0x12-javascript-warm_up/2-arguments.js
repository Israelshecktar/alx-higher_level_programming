#!/usr/bin/node
// A script that prints a message depending on the number of arguments

cont argsNum = process.argv.length - 2;
if (args === 0) {
  console.log('No argument');
} else if (argsNum === 1) {
  console.log('Arguments found');
} else {
  console.log('Arguments found');
}
